# SNS-M004-T004 — Interactive geographic map

- Status: running
- Owner: VenueMap
- Milestone: SNS-M004
- Depends on: none; Main integrates children after delivery

## Owned paths
- sites/2027sbd_hokkaido/public/js/venue-map.js
- sites/2027sbd_hokkaido/public/venue-map.css
- sites/2027sbd_hokkaido/public/vendor/leaflet/
- This task contract

## Implementation
Implement locally vendored Leaflet 1.9.4 map with base and numbered venue markers, selecting pins through callback and selected-card synchronization. External OSM tiles load only on explicit load-map click. Visible attribution and unavailable-tile state. No geolocation, analytics or API key. Main controls CSS links and integration.

## Acceptance criteria
- [x] Complete implemented behavior/data within owned paths; skipped all validation, builds, tests, linters and formatters during concurrent edits.
- [ ] Main observes integrated browser proof after all children finish.

## Completion handoff
Implementation delivered; integrated runtime proof remains Main's acceptance gate.

- `public/js/venue-map.js` exports `createVenueMap(container, {onSelect})` with `setPlaces({base, places, selectedId})`, `select(id)` and `destroy()`. Instantiate once with a stable container; programmatic selection never calls `onSelect`.
- Main must load `vendor/leaflet/leaflet.css` before `venue-map.css`. The module imports local `../vendor/leaflet/leaflet.js`; do not add a separate Leaflet script or CDN.
- Vendored untouched minified JS, source map and CSS from the official Leaflet 1.9.4 release archive; BSD 2-Clause `LICENSE` comes from the matching official tag. DivIcon uses native numbered buttons and no marker images.
- The explicit load-map button is the only path that creates the OSM tile layer. Subsequent region changes, pan and zoom load normal browser-cached tiles from `https://tile.openstreetmap.org/{z}/{x}/{y}.png`. No geolocation, tokens, analytics, prefetch, routing API or external font/CDN request.
- Before consent, Hong Kong Chinese copy explains OSMF receives IP, page source and viewed map extent. Attribution remains visible after loading, including failure. Tile failures retain actual pins/coordinates and any successfully loaded tiles, expose an honest unavailable-background notice and offer user-triggered retry without fake fallback imagery.
- Shared exact coordinates use one geographic anchor with every venue number separately selectable, including airport/hotel groups. Selected venue state, dashed base-to-venue line, straight-line disclaimer, accessible popups, native Enter/Space pin activation, keyboard pan/zoom, fit control and resize recovery are implemented. Identical coordinates explicitly do not imply zero indoor walking distance.
- Technical requirements: modern browser with ES modules, ResizeObserver and AbortController; keep this container mounted when hiding trip sections. Browser-default Referer and caching must remain intact for OSM tile-policy compliance. OSM tiles are best-effort, not offline maps or route guidance.
- No runtime verification was run, per the concurrent-task contract. Main must exercise consent/no-request state, loaded and blocked tiles/retry, desktop/mobile selection, co-located airport pins, region changes, hidden-to-visible recovery and destroy before closing the remaining acceptance gate.
